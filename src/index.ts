import DateSelector, {
  calculateAge,
  formatDate,
  parseDateString,
} from './DateSelector';

export { DateSelector, calculateAge, formatDate, parseDateString };
export type {
  DateSelectorProps,
  DateSelectorTheme,
  LocaleTranslations,
  DateFormat,
  SupportedLanguage,
} from './types';

export default DateSelector;
